@{
    ViewBag.Title = "Книги";
}

@section scripts {
	@Scripts.Render("~/Scripts/angular.js")
	@Scripts.Render("~/Scripts/app/book-controller.js")
}


<div id="bodyContainer" ng-app="BookEditorApp">
	<section id="content">
		<div class="container">
			<div class="row">
				<div class="flip-container text-center col-md-12" ng-controller="BookCtrl" ng-init="getBook()">
					<div class="back" ng-class="{}">
						<p class="lead">{{title}}</p>
						<p>
							<button class="btn btn-info btn-lg next option" ng-click="getBook()" ng-disabled="working">Next Question</button>
						</p>
					</div>
					@*<div class="front" ng-class="{flip: answered}">
						<p class="lead">{{title}}</p>
						<div class="row text-center">
							<button class="btn btn-info btn-lg option" ng-repeat="option in options" ng-click="sendAnswer(option)" ng-disabled="working">{{option.title}}</button>
						</div>
					</div>*@
				</div>
			</div>
		</div>
	</section>
</div>

